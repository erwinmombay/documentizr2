define(function(require) {
    var $ = require('jquery');
    var _ = require('underscore');
    var Backbone = require('backbone');

	var TreeViewPairMediator = Backbone.View.extend({
        initialize: function(options) {
        }
	});
});

